/**
整数 num 通过 +- 1，乘除 2，变更为另一个整数，求最少的变换次数
 */

function changeNum(num: number, target: number): number {
	let res = 0;
	let cur = num;
	if (num > target) {
	} else if (num < target) {
	}
	return res;
}
